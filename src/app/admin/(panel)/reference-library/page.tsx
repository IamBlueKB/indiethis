import ReferenceLibraryClient from "./ReferenceLibraryClient";

export const metadata = { title: "Reference Library — Admin" };

export default function ReferenceLibraryPage() {
  return <ReferenceLibraryClient />;
}
